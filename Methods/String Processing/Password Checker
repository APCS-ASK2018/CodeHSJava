public boolean passwordCheck(String password)
{
    if(password.length() < 8)
        return false;
    boolean match = !password.matches(".*[^0-9a-zA-Z].*");
    System.out.println(password + "\n\t" + match);
    return match;
}
